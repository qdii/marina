/*jslint browser: true*/ /*global  $*/
var calendarProto = {
    on_boat_change: function(id) {
    },

    on_event_click: function(event, jsEvent, view) {
    }
};

var cal = Object.create(calendarProto);
